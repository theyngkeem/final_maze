[project]
name = "mazegen"
version = "1.0.0"
description = "maze generator using dfs"
authors = [
    {name = "yaarab"},
    {name = "otkibou"}
]
requires-python = "3.12.12"
dependencies = [
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
